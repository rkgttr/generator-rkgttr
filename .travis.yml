language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-longtail' ]; then cd .. && eval "mv $currentfolder generator-longtail" && cd generator-longtail; fi

