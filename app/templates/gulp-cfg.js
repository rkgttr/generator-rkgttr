module.exports = function() {
  var config = {
    // you can change the build destination by changing the following path, don't add a forward slash at the end
    buildDir: '<%= buildPath %>'
  };
  return config;
}
